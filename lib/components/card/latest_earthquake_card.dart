import 'package:flutter/material.dart';

class LatestEarthquakeCard extends StatelessWidget {
  const LatestEarthquakeCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Card"),
    );
  }
}
